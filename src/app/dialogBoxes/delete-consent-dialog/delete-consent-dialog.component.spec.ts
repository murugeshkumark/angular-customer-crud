import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeleteConsentDialogComponent } from './delete-consent-dialog.component';

describe('DeleteConsentDialogComponent', () => {
  let component: DeleteConsentDialogComponent;
  let fixture: ComponentFixture<DeleteConsentDialogComponent>;
/*
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeleteConsentDialogComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeleteConsentDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
